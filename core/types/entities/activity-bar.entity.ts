/**
 * Primary sidebar activities. Values are persisted in the activity-bar store,
 * so renaming one requires a migration.
 */
export enum ActivityBarItemType {
  Explorer = 'Explorer',
  Schemas = 'Schemas',
  ErdDiagram = 'ERDiagram',
  UsersRoles = 'UsersRoles',
  DatabaseTools = 'DatabaseTools',
  Agent = 'Agent',
}
